# Basic

## 멤버(member)
> class에 속해 있는 모든 구성요소를 뜻한다.

- 멤버 변수(field) : 클래스가 가지고 있는 데이터(상태)
- 멤버 메서드(method): 클래스가 할 수 있는 행동 (기능)
- 생성자(constructor): 객체를 생성할 때 호출되는 메서드
- 내부 클래스

## 초기화(initialization)
> 변수나 객체가 생성하고 **처음 값을 지정**해서 사용 할 수 있게 만드는 과정